// Hand-curated futureStem (लृट् stem) for in-Gītā dhātus that the
// bulk-extended parts didn't include. This file is overwritten by the
// futureStem agent batch when it completes — until then, the empty
// export means hydrate.js's futureStem-fill pass is a no-op.
//
// Each entry maps the dhātu's ASCII id (e.g. 'bhu', 'gam') to its
// futureStem in Devanagari. The conjugator appends लृट् personal
// endings (-ति, -ष्यन्ति, etc.) to produce the full future-tense form.
export const FUTURE_STEMS = {};
